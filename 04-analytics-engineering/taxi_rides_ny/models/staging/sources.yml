version: 2

sources:
  - name: raw_data
    description: "Raw data sources for NYC taxi rides"
    database: kestra-sandbox-485208 # Project ID    ( if Use BigQuery)
    schema: zoomcamp                # Dataset Name  ( if Use BigQuery)  
    tables: 
      - name: yellow_tripdata_partitioned
      - name: green_tripdata
